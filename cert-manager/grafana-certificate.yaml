apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-lab-tls
  namespace: monitoring
spec:
  secretName: grafana-lab-tls
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - grafana.lab
