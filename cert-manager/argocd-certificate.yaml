apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-lab-tls
  namespace: argocd
spec:
  secretName: argocd-lab-tls
  issuerRef:
    name: homelab-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - argocd.lab
